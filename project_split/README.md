# Project Split

- `without_library/`: folders implemented as standalone/non-library transpile or experiment workflows.
- `with_library/`: folders implemented using the ampamp library modules.

This is a copied organization layer; original folders in project root are unchanged.
